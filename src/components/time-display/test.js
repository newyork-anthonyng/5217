var expect = require('chai').expect;

test('should render time in milliseconds as minutes', function(context) {
  // var timeInMs = 2 * 60 * 1000; // 2 minutes
  // var output = context.render({ timeInMs: timeInMs });
  // console.log(output.$('h1').html);
  // console.log(output.$('h1').text);
  // // console.log(output);
  // // console.log(output.html);
  // // console.log(output.text);
  // // expect(output.html).to.equal('<h1>2</h1>');
});
